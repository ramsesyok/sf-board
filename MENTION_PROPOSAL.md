# メンション候補表示の設計案

## 現状

- `users/{userId}.json` がユーザ登録の実体である。`readAllUserProfiles()` は `users/` を列挙し、全プロフィールを読む。
- `ChatModel` は起動時とチャンネル更新時にプロフィールを読み、`getUsers()` でメモリ上の一覧を返す。
- Host は `init` / `channelUpdated` でその一覧を Webview に渡し、Webview も `users` マップを保持している。
- 本文中の既知の `@userId` は画面上で `@DisplayName` に変換し、自分宛のみ強調する。メンション通知は対象外。
- 現在は `@` 入力に対する候補表示がない。また、プロフィールだけが追加・変更されてもチャンネルイベントが発生しなければ既存パネルの一覧は更新されない。

## 推奨方針

共有フォルダに全員が更新する「メンバー一覧ファイル」は作らない。`users/` のファイル名をユーザ集合の原本とし、Host の `ChatModel` に一覧を保持する。Webview は受け取った `users` マップから入力候補を検索する。`@` の入力・候補の絞り込み・選択では共有フォルダにアクセスしない。

共有の集約ファイルを置くと、複数ユーザからの更新競合や、共有フォルダに派生状態を置かない設計原則に抵触する。現在の想定規模は最大約 50 人で、候補のメモリ内検索に別の索引は不要。

### 一覧の更新

1. 初期化時に従来どおり `users/` を列挙し、プロフィールを読み込む。
2. 以後は候補操作と独立した低頻度の照合（案: 約 90 秒、ジッタ付き）で `users/` を列挙する。前回のファイル名と `mtime` / サイズを保持し、新規・変更ファイルのみ読み直す。さらに、タイムスタンプの粒度による同サイズ更新の見落としを避けるため、長い間隔（案: 15 分）で全プロフィールを読み直す。消えたファイルは候補から外す。読取失敗時は前回の一覧を維持して次回再試行する。
3. プロフィール一覧が変わったら `ChatModel` が専用の `usersUpdated` を通知し、開いている各 Webview に `usersUpdated` メッセージで一覧を送る。全メッセージの再送は行わず、候補と既存 DOM の表示名・メンション title 等の必要箇所を更新する。
4. パネルを開いた直後は Host が持つ最新の一覧を即座に渡す。必要なら、有効期限が切れた場合のみ Host で照合を予約する。

照合は既存のカーソル監視と別の低頻度処理とする。`cursors/` だけを監視する方針を維持し、`users/` に新しい `fs.watch` は設けない。新規ユーザが投稿前でも候補に現れ、表示名の変更も反映できる。

### 入力 UI

- メイン、スレッドペイン、インライン返信の各テキストエリアで同じ候補処理を使う。
- 単語境界にあるカーソル直前の `@` とその後の `userId` 用文字列を検出する（メールアドレス内は対象外）。空の `@` では全候補を表示し、入力に応じて `userId` と `displayName` を部分一致で絞る。候補には表示名と `userId` の両方を示す。
- 候補は表示名順、上限 10 件。矢印キーで移動、Enter / Tab / クリックで選択、Esc で閉じる。候補が開いていない Enter は従来どおり改行、Ctrl+Enter は従来どおり送信。
- 選択時は入力欄の該当トークンを `@DisplayName` に置換し、カーソルを直後へ移す。同時に選択した `userId` とトークン位置を入力欄ごとのローカル状態に記録する。本文の保存値は既存の `@userId` とする。
- 入力・削除に伴って位置を更新し、選択済みトークンの中身が編集されたらその紐付けを外す。送信時は残る紐付けを後ろから `@userId` へ変換して送る。表示名が重複しても送信先は候補選択時の `userId` で決まる。コピーした単なる `@DisplayName` テキストは、候補から再選択しない限り自動では ID に変換しない。
- 送信後に表示するチャット本文では、既存の `@userId` を現在の `@DisplayName` に変換する。自分宛のみ色を変え、他ユーザー宛は通常の本文色にする。表示名変更時も紐付け先の ID は変わらない。
- IME の変換中は候補確定キーを処理しない。カーソル移動、空白、削除、入力欄のフォーカス喪失で候補を閉じる。
- 一覧が未取得・更新失敗中でも通常のテキスト入力と送信は可能にする。

### アクセス量

| 操作 | 共有フォルダへの追加アクセス |
|---|---:|
| `@` 入力、絞り込み、候補選択 | 0 |
| 初期化 | 従来と同じ `users/` 列挙とプロフィール読込 |
| 定期照合 | `users/` 列挙とファイルのメタデータ確認。通常は新規・変更ファイルのみ読み、長い間隔で全件を照合 |

現在の `refreshChannel()` はチャンネルのイベントが変わるたびに全プロフィールを読み直す。この読込を専用の一覧照合へ移せば、チャンネル更新時のプロフィール読込も減らせる。

## 設計文書へ反映する差分（実装前に合意する事項）

- `DESIGN.md` §5: `users/` の低頻度照合を追加。ただし更新監視対象は引き続き `cursors/` のみとし、入力操作では I/O しないことを明記。
- `DESIGN_EXTENSION.md` §5: Host → Webview の `usersUpdated` と、Host 内のユーザ一覧変更通知を追加。
- `DESIGN_EXTENSION.md` §6: 3 種の入力欄に共通する候補の表示・選択・IME・キーボード操作を規定。
- `DESIGN_EXTENSION.md` §10 / §14: 入力欄では `@DisplayName` を表示し、送信時に既存の `@userId` 表記へ変換することを明記。チャット本文も表示名に変換し、自分宛だけ強調する。メンション通知は追加しない。

## 確認する境界条件

- 現在の `resolveUserId()` は設定値をそのまま採用する一方、既存のメンション装飾は `[A-Za-z0-9_-]` のみを認識する。まず既存プロフィールの `userId` を確認し、この文字集合外の ID が実際に使われている場合は、安全な表記・既存データとの互換性を先に設計する。既存 ID を候補選択時に黙って正規化しない。
- `users/` に同じ表示名のプロフィールがある場合も `userId` で識別する。
- 新規ユーザが投稿していなくても、次の一覧照合後に候補に表示される。
- プロフィールだけを更新した場合も、開いているパネルの候補と本文の hover 表示が更新される。
- 共有フォルダが一時的に読めなくても既存の候補を維持し、復旧後に追いつく。
- スレッド返信、IME 変換、Ctrl+Enter 送信が候補 UI に妨げられない。
